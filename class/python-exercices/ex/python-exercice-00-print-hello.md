---
title: "Python 00 - Print Hello (Template)"
---

# Print Hello

This is the **template** exercise. Use it as a model for new exercises.

## Instructions

Write a function `hello(name: str) -> str` that returns the string `"Hello, <name>!"`.

## Starter code

```python # template
def hello(name: str) -> str:
    """Return a greeting string."""
    # YOUR CODE HERE
```

## Tests

```python # tests
# Automated tests - do not modify
assert hello("World") == "Hello, World!", f"Got: {hello('World')}"
assert hello("Python") == "Hello, Python!", f"Got: {hello('Python')}"
assert hello("") == "Hello, !", f"Got: {hello('')}"
print("All tests passed!")
```
