* [X] In markdown vierwer.js, add a button to code block to copy everything to the clipboard.
* [X] Fix spelling in @blog/opencode-automation.md and reformulate well not gramatically corret, but keep my style.
* [X] Find a way to avoid serving some files, `doc/ia-*` in gh-pages. And explain how you did it: I guess I would still liek to commit them for liek a changelong but to not serve them is that possible? Report in @doc/gen/feedback-no-gh-page.txt.
* [X] Alter the copy button to somethign with an emoji, like the image of two pages on antoher with a small shift. Also put the button at a max of 1000 pixel right, I <min at a max of max with. Currently it is fare right at the end of the page, which was not well written by you. Not very clever.
* [X] Add a Markdown macro so I can embed code from another GitHub page directly into my blog (uses a ```embed fenced block; markdown_viewer.js fetches the raw file + optional #L10-L30 range and highlights it with Prism). Feedback at OpenClaudeMaster/doc/ref/feedback-embed-code-in-markdown-viewer.md.

